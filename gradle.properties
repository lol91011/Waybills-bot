# Включение современных библиотек AndroidX
android.useAndroidX=true

# Выделение 3 ГБ оперативной памяти для компиляции Compose/DEX
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=512m

# Отключение фонового демона (экономит RAM на серверах GitHub)
org.gradle.daemon=false
